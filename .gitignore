# Private statements and other sensitive data
sensitive/

# PII pseudonym mapping stores contain the original PII — never commit
pii_map.json
*.pii-map.json

# Downloaded model weights
models/hf-cache/

# Generated synthetic eval corpora (regenerable: python -m pii_eval generate)
pii_eval/corpus*/

# Python bytecode cache (analyze_statement imports verify_examples)
__pycache__/

# ChunkHound semantic-search index (local, regenerable) + local tool config
.chunkhound/
.chunkhound.json
