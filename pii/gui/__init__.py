"""BrokerAI PII GUI front-end (planned — stub).

A local, interactive front-end over ``pii.core``. Not yet implemented; see
pii/gui/ARCHITECTURE.md, ROADMAP.md and TODO.md for the plan. Depends only on
``pii.core`` — must never import ``pii.cli``.
"""
