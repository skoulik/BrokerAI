# PII stripping tool (Phase 1) — separate from the RAG app's deps.
# After installing, fetch the spacy model:
#   python -m spacy download en_core_web_sm
# The GLiNER2 model (~1.2 GB) downloads itself into models/hf-cache on first use.
presidio-analyzer>=2.2.363
presidio-anonymizer>=2.2.363
gliner2>=1.3.2
# Image path (strip --image): PaddleOCR is the OCR engine. It needs a
#   paddlepaddle wheel too, chosen per machine and NOT pinned here:
#   - GPU (paddlepaddle-gpu, CUDA) is the fast path; on Windows it cannot
#     share a process with torch, so the pipeline drives it through a
#     worker subprocess (pii/core/ocr_worker.py).
#   - CPU (paddlepaddle) works in-process but is slow; see pii/core/ocr_paddle.py.
paddleocr>=3.7
Pillow>=10.1
# PDF mode: page rendering (and later reassembly/text-layer/metadata access).
# NOTE pymupdf is AGPL — fine for internal use; revisit the license (Artifex
# commercial, or swap the pdf_mode seam to pypdfium2) before any commercial
# distribution. Decision 2026-07-17, pii/core/ARCHITECTURE.md.
pymupdf>=1.28
