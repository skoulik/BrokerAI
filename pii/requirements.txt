# PII stripping tool (Phase 1) — separate from the RAG app's deps.
#
# DETECTION NEEDS A MODEL SERVER, not a package: layer 0 is a local LLM reached
# over HTTP (llama-server; see pii/README.md and $PII_VLM_URL). Nothing here
# installs or starts it, and pii.core's transport is stdlib-only on purpose.
#
# Retired, and deliberately absent: gliner2 + torch (layer 2, 2026-08-09 —
# layer 0 replaced it), presidio-analyzer + spaCy (the detection chassis,
# 2026-08-09 — pii/core/engine.py replaces it). The chassis swap is what made
# the process torch-free and let the paddle worker subprocess go with it;
# re-adding any of them brings back the paddle-GPU DLL conflict.

# Layer 1: the regex engine, the two validators worth not writing ourselves.
#   regex   — variable-length lookbehind, which stdlib `re` cannot compile and
#             the account-after-BSB patterns need.
#   tldextract    — public-suffix validation for email addresses.
#   phonenumbers  — the libphonenumber port behind the AU phone rule.
regex>=2024.5
tldextract>=5.1
phonenumbers>=8.13

# Image path (strip --image): PaddleOCR is the OCR engine, supplying paint
#   GEOMETRY only, never detection. It needs a paddlepaddle wheel too, chosen
#   per machine and NOT pinned here — GPU (paddlepaddle-gpu, CUDA) is the fast
#   path, CPU (paddlepaddle) is slower. Both run in-process; see
#   pii/core/ocr_paddle.py for the DLL rules that still apply if anything
#   re-introduces torch.
paddleocr>=3.7
Pillow>=10.1

# PDF mode: page rendering, reassembly, text-layer/metadata access.
# NOTE pymupdf is AGPL — fine for internal use; revisit the license (Artifex
# commercial, or swap the pdf_mode seam to pypdfium2) before any commercial
# distribution. Decision 2026-07-17, pii/core/ARCHITECTURE.md.
pymupdf>=1.28
